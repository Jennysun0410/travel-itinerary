## Tasks

- [x] Add `scanGmailByDateRange(userId: string, from: string, to: string): Promise<{ imported: number; skipped: number }>` to `packages/api/src/email/gmail.ts`. Build Gmail query string as `subject:(booking confirmation OR reservation OR order confirmation OR e-ticket) after:YYYY/MM/DD before:YYYY/MM/DD` using the from/to params (replace `-` with `/`). Call `gmail.users.messages.list` with `maxResults: 50`. For each message, call `gmail.users.messages.get` and `extractEmailText`, then call `enqueueEmailForParsing`. Count messages where `raw_email_id` already existed in DB as `skipped`, newly created as `imported`. Return `{ imported, skipped }`.

- [x] Create `packages/api/src/routes/email-scan.ts` with `POST /gmail/scan` route. Require auth. Validate request body has `from` and `to` as strings matching `/^\d{4}-\d{2}-\d{2}$/` and that `from <= to`; return 400 `validation_error` if not. Query `SELECT access_token, refresh_token, token_expiry FROM email_connections WHERE user_id = $1 AND provider = 'gmail'`; return 400 `no_gmail_connection` if no rows. Set OAuth2 credentials from decrypted tokens, call `scanGmailByDateRange(userId, from, to)`, return `{ imported, skipped }` with status 200.

- [x] Register the new email-scan router in `packages/api/src/index.ts`: import `emailScanRouter` from `./routes/email-scan` and add `app.use('/email', emailScanRouter)` after the existing email router.

- [x] Update `packages/web/src/app/onboarding/page.tsx`: add `step` state (initial value `1`). After successful PATCH /users/me (username save), set `step` to `2` instead of calling `router.replace('/trips')`. In the render, when `step === 2`, show a new screen: heading "連結 Gmail 以自動匯入訂單確認信", subtext "連結後，系統將自動掃描訂單確認信並加入行程", two buttons — "連結 Gmail" (`<a href={getApiUrl('/email/gmail/connect')}`) and "略過" (`onClick={() => router.replace('/trips')`). Import `getApiUrl` from `../../../lib/api`.

- [x] Update `packages/web/src/app/trips/[id]/orders/page.tsx`: add `addMode` state with values `null | 'email' | 'manual'`. Add `gmailConnected` state (boolean, default false). On mount, call `apiFetch<EmailConnection[]>('/email/connections')` and set `gmailConnected` to whether any connection has `provider === 'gmail'`. Change the `+ Add Order` button to set `addMode` to `null` if already open, else show two buttons "從信箱匯入" and "手動新增". When `addMode === 'manual'`, show the existing form (unchanged). When `addMode === 'email'`, show: if not gmailConnected, show "尚未連結 Gmail — <a href='/settings/email'>前往設定</a>"; if connected, show two date inputs (from/to, type="date") and "開始匯入" button. On submit call `POST /email/gmail/scan` with `{ from, to }`, show loading, on success show "已匯入 N 筆訂單" and call `loadOrders()`. Import `EmailConnection` from `@travel/shared`.
