## Tasks

- [x] Run DB migration: create `packages/api/src/db/migrate-destinations.ts` that connects to the DB via `packages/api/src/db/client.ts`, executes `ALTER TABLE trips ADD COLUMN IF NOT EXISTS destinations JSONB NOT NULL DEFAULT '[]'`, executes `ALTER TABLE orders ADD COLUMN IF NOT EXISTS booking_date TEXT`, then for each existing trip with a non-null `destination` text, updates `destinations` to `[{"name": <destination>, "timezone": <guessTimezoneFromDestination(destination)>, "startDate": <start_date>, "endDate": <end_date>}]`. Run via `npx tsx packages/api/src/db/migrate-destinations.ts`.

- [x] Update `packages/api/src/routes/trips.ts` POST handler: replace the `{ name, destination, start_date, end_date }` destructure with `{ name, destinations }` where `destinations` is `Array<{ name: string; timezone: string; startDate: string; endDate: string }>`. Validate that each destination has all four fields. Compute `start_date` as `destinations.length ? Math.min(...destinations.map(d => new Date(d.startDate).getTime()))` converted to YYYY-MM-DD, `end_date` similarly. Set `destination` as `destinations[0]?.name ?? ''`. Insert `destinations` JSONB into the new column. Return the full trip including `destinations` in the response.

- [x] Update `packages/api/src/email/parser.ts`: add `bookingDate: string | null` to the parser output type. In the parse function, after determining `type`, use regex to extract dates: for `flight`, match `/Departure[:\s]+([A-Za-z]+ \d+, \d{4}|\d{4}-\d{2}-\d{2})/i`; for `accommodation`, match `/Check-in[:\s]+([A-Za-z]+ \d+, \d{4}|\d{4}-\d{2}-\d{2})/i`; for `activity`, match `/Date[:\s]+([A-Za-z]+ \d+, \d{4}|\d{4}-\d{2}-\d{2})/i`. Parse matched string to ISO YYYY-MM-DD using `new Date(...).toISOString().split('T')[0]`. Set `bookingDate = null` if no match. Pass `bookingDate` through to the order INSERT in `packages/api/src/routes/orders.ts`, storing it in the `booking_date` column.

- [x] Update `packages/web/src/shared/datetime.ts`: add `export function resolveTimezone(bookingDate: string | null, destinations: Array<{ timezone: string; startDate: string; endDate: string }>): string`. Implementation: if `bookingDate` is null, return `Intl.DateTimeFormat().resolvedOptions().timeZone`. Sort destinations by startDate ascending. Return the timezone of the first destination where `startDate <= bookingDate <= endDate`. If no match, return `Intl.DateTimeFormat().resolvedOptions().timeZone`.

- [x] Update `packages/web/src/shared/types.ts`: add `TripDestination` interface `{ name: string; timezone: string; startDate: string; endDate: string }`. Add `destinations: TripDestination[]` field to the `Trip` type.

- [x] Rewrite `packages/web/src/app/trips/new/page.tsx`: center the layout with `maxWidth: 480, margin: '0 auto'`. Replace single destination input with a dynamic list of destination rows. Each row has: a `<select>` populated from `CITY_OPTIONS` constant (defined in this file: array of `{ label: string, value: string, timezone: string }` for the 13 cities listed in the spec), a start date input, an end date input, and a remove button. Add an "＋ 新增目的地" button that appends a new empty row. Remove the `start_date` and `end_date` top-level inputs (auto-derived). On submit, POST `{ name, destinations: [{name, timezone, startDate, endDate}] }`.

- [x] Update `packages/web/src/app/trips/[id]/timeline/page.tsx`: import `resolveTimezone` from `packages/web/src/shared/datetime.ts`. For each order rendered, call `resolveTimezone(order.booking_date ?? null, trip.destinations ?? [])` to get the display timezone. Pass this timezone to `LocalDatetime` instead of passing the trip destination string.

- [x] Commit and push all changes: stage all modified and new files, commit with message `feat: multi-destination trips with per-order timezone and email date extraction`, push to main.
