## 1. Refactor parser.ts: parseEmail 改為 return 而非 INSERT — Parse order details from email (Two-endpoint split)

- [x] 1.1 In `packages/api/src/email/parser.ts`, change `parseEmail` signature to `parseEmail(userId: string, emailId: string, body: string, tripDateRange?: { start: string; end: string }): Promise<ParsedOrder | null>`. Remove the `pool.query INSERT` from inside `parseEmail` — instead return a `ParsedOrder` object `{ raw_email_id: emailId, type, vendor, booking_ref, start_datetime, end ?? new Date().toISOString(), end_datetime, price, currency, flagged_for_review }` or `null` if `isBookingBody` returns false or tripDateRange filtering excludes it. Export the `ParsedOrder` type from this file. This implements the ParsedOrder response shape.
- [x] 1.2 Update `enqueueEmailForParsing` in `packages/api/src/email/parser.ts` to call the refactored `parseEmail` and, if a non-null result is returned, INSERT into the database with `trip_id = NULL` (preserving the existing push-notification flow for parse order details from email via push). The duplicate-check `SELECT 1 FROM orders WHERE raw_email_id = $1` stays inside `enqueueEmailForParsing` before calling `parseEmail`.

## 2. Preview parsed orders before importing — Gmail scan update

- [x] 2.1 In `packages/api/src/email/gmail.ts`, add `scanGmailForPreview(userId: string, from: string, to: string, tripDateRange?: { start: string; end: string }): Promise<ParsedOrder[]>`. This function reuses the same Gmail query logic as `scanGmailByDateRange`, but for each message calls `parseEmail` instead of `enqueueEmailForParsing`, collects non-null results, and returns the array without writing to DB. Import `parseEmail` and `ParsedOrder` from `./parser`. This satisfies the "Preview parsed orders before importing" requirement.

## 3. New API endpoints — Two-endpoint split: /preview and /import

- [x] 3.1 Create `packages/api/src/routes/email-preview.ts` implementing the two-endpoint split with ParsedOrder response shape:
  - `POST /gmail/preview`: require auth; validate `from`, `to` (YYYY-MM-DD, from <= to), and `trip_id` (string); query trip date range from DB; call `scanGmailForPreview`; return the ParsedOrder array as JSON. Return 400 `no_gmail_connection` if no Gmail token found. Return 400 `validation_error` if date params invalid.
  - `POST /gmail/import`: require auth; validate `trip_id` (string) and `orders` (non-empty array); for each order in `orders`, run `INSERT INTO orders (trip_id, created_by, type, vendor, booking_ref, start_datetime, end_datetime, price, currency, raw_email_id, flagged_for_review, status) VALUES ($1...) ON CONFLICT (raw_email_id) DO NOTHING`; count rows with `rowCount === 1` as imported; return `{ imported: N }`. This satisfies "Confirm and import selected orders".
- [x] 3.2 Register the new router in `packages/api/src/index.ts`: `import emailPreviewRouter from './routes/email-preview'` and add `app.use('/email', emailPreviewRouter)` after the existing email routers.

## 4. Frontend: Orders page preview flow — Confirm and import selected orders

- [x] 4.1 In `packages/web/src/app/trips/[id]/orders/page.tsx`, add state: `previewOrders: ParsedOrder[]`, `selectedIds: Set<string>` (keyed by `raw_email_id`), `importing: boolean`. Add a `ParsedOrder` type matching the backend shape `{ raw_email_id, type, vendor, booking_ref, start_datetime, end_datetime, price, currency, flagged_for_review }`. This initiates the Orders page preview flow.
- [x] 4.2 Replace the existing `handleScan` function: on form submit call `POST /email/gmail/preview` with `{ from: scanFrom, to: scanTo, trip_id: params.id }`. On success set `previewOrders` to the response array and initialize `selectedIds` to a Set of all `raw_email_id` values (select-all by default). On error show the error message. Do not set `scanResult` text yet. This satisfies "Preview parsed orders before importing" on the frontend.
- [x] 4.3 Add `handleImport` async function: set `importing = true`; call `POST /email/gmail/import` with `{ trip_id: params.id, orders: previewOrders.filter(o => selectedIds.has(o.raw_email_id)) }`; on success set `scanResult` to `已匯入 ${result.imported} 筆訂單`; call `loadOrders()`; clear `previewOrders` and `selectedIds`; set `importing = false`. This satisfies "Confirm and import selected orders".
- [x] 4.4 In the email import panel JSX, add a preview list section for the Orders page preview flow that renders when `previewOrders.length > 0`. Each row shows: checkbox (checked if `selectedIds.has(raw_email_id)`, toggles the Set on change), vendor, type, start_datetime (date portion only), price + currency, and a "待確認" badge if `flagged_for_review`. Below the list show a "全選/取消全選" toggle and a "確認匯入" button (disabled when `selectedIds.size === 0` or `importing`).
- [x] 4.5 When `previewOrders` is empty and scan completes (no results), show inline message "未找到符合條件的訂單" instead of the preview list.
